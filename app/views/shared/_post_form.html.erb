<%= form_for @post do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :text, placeholder: "What are you thinking about?" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>